//Creating a default race selection, but also making these variable so they can be manipulated later

let raceState = 'terran';

let dataManipulator = new dataHandler(raceState);
let figureTool = new figureCreator();